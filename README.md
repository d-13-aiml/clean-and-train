pip install pandas numpy scikit-learn python.
-m pip install pandas numpy scikit-learn. 
Run this code to install the required packages.
The cleaned_ipl_data_new.csv dataset is highly prepared and ready to predict a win between two teams using the techniques we have already established (Random Forest, Logistic Regression, etc.).
